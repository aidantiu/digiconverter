// Passport configuration (Google OAuth removed for now)
const passport = require('passport');

// Google OAuth has been temporarily removed
console.log('ℹ️  Google OAuth disabled - using email/password authentication only');

module.exports = passport;
